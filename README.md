# computercreativty

Should modify the variable tag at the main part in code.py to determine which category of music you want to generate.

Should run train(tag) to get the best weights when the frist time to run.

The weights file is too large to upload, over the limitation of 25MB.
